[project]
name = "political-economy-reader"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "kokoro>=0.9.2",
    "sounddevice",
    "torch",
    "numpy",
    "rich",
    "huggingface-hub",
]
